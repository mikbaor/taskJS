/**
 * Crea un objeto persona con las propiedades nombre, edad y ciudad y retorna el objeto
 */
function crearPersona(nombre, edad, ciudad) {

}


/**
 * Devuelve o retorna el valor de la propiedad especificada de un objeto.

 */

function obtenerPropiedad(objeto, propiedad) {

}

/**
 * Agrega una propiedad con el valor especificado a un objeto.
 * Si la propiedad ya existe, actualiza su valor.
 * Retorna el objeto modificado.
 */
function agregarPropiedad(objeto, propiedad, valor) {

  return objeto;
}

/**
 * Elimina la propiedad especificada de un objeto.
 * Retorna el objeto modificado.
 */
function eliminarPropiedad(objeto, propiedad) {

}

/**
 * Verifica si el objeto tiene una propiedad con el nombre especificado.
 * Retorna true si la propiedad existe, false en caso contrario.
 */
function tienePropiedad(objeto, propiedad) {
 
}


/**
 * Cuenta el n mero de propiedades de un objeto.
 * Retorna el n mero de propiedades.
 */
function contarPropiedades(objeto) {
  
}

/**
 * Crea una copia superficial de un objeto y devuelve o retorna el nuevo objeto.
 */

function copiarObjeto(objeto) {

}

/**
 * Fusiona dos objetos en uno solo.
 * Crea un nuevo objeto con las propiedades de los dos objetos.
 * Retorna el nuevo objeto.
 */
 function fusionarObjetos(obj1, obj2) {

}

// Pruebas
module.exports = {
  crearPersona,
  obtenerPropiedad,
  agregarPropiedad,
  eliminarPropiedad,
  tienePropiedad,
  contarPropiedades,
  copiarObjeto,
  fusionarObjetos
};